{
  "name": "resume-screening-candidate-ranking-system",
  "version": "1.0.0",
  "description": "ATS Resume Screening & Candidate Ranking System Root Workspace",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" -n \"backend,frontend\" -c \"cyan,magenta\"",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run build:backend && npm run build:frontend"
  },
  "devDependencies": {
    "concurrently": "^10.0.0"
  }
}
